import React from 'react'
import Figure from 'react-bootstrap/Figure'
import { } from '../../Styles/Pages/AboutPage.Styles'

const ImagesPerState = ({ img, url, loading }) => {

  if (loading) {
    return <h2>Loading...</h2>;
  }

  return (
    <>
    
    </>
  )
}

export default ImagesPerState
